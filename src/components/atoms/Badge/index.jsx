import styled from "styled-components"

export const AbilityBadge = styled.p`
        color: #FFF;
        font-size: 14px;
        padding: 8px 22px;
        border-radius: 8px;
        background: blue;
        height: fit-content;
    `